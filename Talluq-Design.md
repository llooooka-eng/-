# تألق (Talluq) — دليل التصميم الكامل
**Talluq — Beauty & Grooming Booking App · Design Specification**

تطبيق حجز مواعيد صالونات الحلاقة والتجميل (رجال، نساء، أطفال، خدمة منزلية).
نظام تصميم عربي أولاً (RTL)، ثنائي اللون: لؤلؤي فاتح + حبري داكن، مع لمسة ذهبية واحدة («تألق» = يلمع).

---

## 1. الهوية والمبادئ

- **الاسم:** تألق (Talluq) — يعني «يلمع / يتألق».
- **الاتجاه:** RTL (عربي أولاً). لا تباعد أحرف (letter-spacing) على النص العربي — يكسر الاتصال.
- **المزاج:** فخم، هادئ، نظيف. أسطح لؤلؤية دافئة + دُكن حبرية + ذهبي واحد للإبراز فقط.
- **الذهبي مقتصد:** يُستخدم للتقييمات، التحديد، الإبراز والميدالية — لا للمساحات الكبيرة.
- **الحركة:** ناعمة وقصيرة. منحنيات `ease-standard` / `ease-emphasis`.

---

## 2. الألوان (Color Tokens)

### السلالم الأساسية
| الفئة | التوكنات |
|---|---|
| **Pearl** (أسطح دافئة) | `--pearl-0 #FFFFFF` · `--pearl-50 #FAF9F6` (اللؤلؤي المميّز) · `--pearl-100 #F4F3EE` · `--pearl-200 #ECEAE2` |
| **Ink** (محايد شبه أسود) | `--ink-950 #0E0F13` · `--ink-900 #16181E` · `--ink-800 #1F222A` · `--ink-700 #2B2F3A` · `--ink-600 #3C4150` · `--ink-500 #565C6B` · `--ink-400 #767D8D` · `--ink-300 #A0A6B2` · `--ink-200 #C9CCD4` · `--ink-100 #E4E6EB` · `--ink-50 #F0F1F4` |
| **Gold** (إبراز / تألق) | `--gold-700 #8A6D18` · `--gold-600 #A8851C` · `--gold-500 #C4A032` (الذهبي المميّز) · `--gold-400 #D8BB57` · `--gold-300 #E7D289` · `--gold-200 #F1E4B8` · `--gold-100 #F8F1DA` |

### ألوان دلالية
- **أخضر (نجاح):** `--green-600 #157A4C` · `--green-500 #1E9E63` · `--green-100 #DFF3E8`
- **كهرماني (تحذير):** `--amber-600 #B5730A` · `--amber-500 #D68B12` · `--amber-100 #FBEFD6`
- **أحمر (خطر):** `--red-600 #C13636` · `--red-500 #DC4C4C` · `--red-100 #FBE3E3`
- **أزرق (معلومة):** `--blue-600 #2360A6` · `--blue-500 #2F7BD1` · `--blue-100 #E1EDFA`

### الأسماء الدلالية (Light theme)
| الدور | القيمة |
|---|---|
| `--surface-page` | pearl-50 |
| `--surface-card` | pearl-0 |
| `--surface-sunken` | pearl-100 |
| `--surface-inverse` | ink-900 |
| `--text-primary` | ink-900 |
| `--text-secondary` | ink-500 |
| `--text-muted` | ink-400 |
| `--border-subtle` | ink-100 |
| `--border-default` | ink-200 |
| `--border-strong` | ink-300 |
| `--brand` (أزرار رئيسية) | ink-900 · hover ink-800 · contrast pearl-50 |
| `--accent` (ذهبي) | gold-500 · hover gold-600 · soft gold-100 · text gold-700 |
| `--success / warning / danger / info` | green-500 / amber-500 / red-500 / blue-500 (+ `-soft` بالفاتح) |
| `--focus-ring` | gold-400 |
| `--overlay` | rgba(14,15,19,0.48) |

> يوجد **ثيم داكن** (`[data-theme="dark"]`) يعيد تعيين الأسماء الدلالية فقط: الأسطح تصير حبرية، `--brand` يصير لؤلؤي، والذهبي يخفّ إلى gold-400.

---

## 3. الخطوط والطباعة (Typography)

- **الخط الأساسي:** `IBM Plex Sans Arabic` (عربي + عرض)، يليه `IBM Plex Sans` للّاتيني، ثم system-ui.
- **مونو:** `IBM Plex Mono` للأرقام والأكواد.
- **المصدر:** Google Fonts — أوزان 300/400/500/600/700.

### سلّم الأحجام
`2xs 11` · `xs 12` · `sm 13` · `base 15` (نص UI افتراضي) · `md 16` · `lg 18` · `xl 22` · `2xl 28` · `3xl 34` · `4xl 44` · `5xl 56` (px)

### الأوزان
light 300 · regular 400 · medium 500 · semibold 600 · bold 700

### ارتفاعات السطر
tight 1.2 · snug 1.35 · normal 1.55 (العربي يُقرأ أفضل أوسع قليلًا) · relaxed 1.75

### الأدوار الدلالية
| التوكن | القيمة |
|---|---|
| `--font-display` | bold 44/1.2 |
| `--font-title` | semibold 28/1.35 |
| `--font-heading` | semibold 22/1.35 |
| `--font-subhead` | medium 18/1.35 |
| `--font-body` | regular 15/1.55 |
| `--font-body-strong` | semibold 15/1.55 |
| `--font-caption` | regular 13/1.35 |
| `--font-label` | medium 13/1.35 |
| `--font-overline` | semibold 12/1.35 |

> Tracking (`--tracking-*`) للّاتيني/العرض فقط — لا يُطبَّق على العربي.

---

## 4. المسافات والأبعاد (Spacing)

- **شبكة أساس 4px.** السلّم: `space-1 4` → `space-2 8` → `3 12` → `4 16` → `5 20` → `6 24` → `8 32` → `10 40` → `12 48` → `16 64` → `20 80` → `24 96`.
- **التخطيط:** `--container-sm 420px` (عرض شاشة الجوال) · `md 768` · `lg 1080` · `--gutter-page 20px` (هامش جانبي للجوال).
- **ارتفاعات العناصر (هدف لمس ≥ 44px):** `--control-sm 36` · `--control-md 44` · `--control-lg 52`.
- **أيقونات:** `--icon-sm 16` · `--icon-md 20` · `--icon-lg 24`.

---

## 5. الزوايا والظلال والحركة

### الزوايا (Radius)
`xs 6` · `sm 10` · `md 14` (افتراضي للبطاقات/العناصر) · `lg 20` · `xl 28` · `2xl 36` · `full 999`

### الظلال (Elevation — ناعمة، بميل حبري)
- `--shadow-xs` 0 1px 2px rgba(14,15,19,.04)
- `--shadow-sm` 0 1px 3px + 0 1px 2px
- `--shadow-md` 0 4px 12px rgba(14,15,19,.08)
- `--shadow-lg` 0 12px 32px rgba(14,15,19,.10)
- `--shadow-xl` 0 24px 60px rgba(14,15,19,.14)
- `--shadow-focus` 0 0 0 3px var(--focus-ring)

### الحركة (Motion)
- `--ease-standard` cubic-bezier(0.2,0,0,1)
- `--ease-emphasis` cubic-bezier(0.3,0,0,1)
- `--ease-out` cubic-bezier(0,0,0.2,1)
- المُدد: `--dur-fast 120ms` · `--dur-normal 200ms` · `--dur-slow 320ms`

---

## 6. اللوقو والميدالية

- **اللوقو:** رمز حلاق/عائلة أبيض على خلفية داكنة (`talluq/logo-t.png` شفاف، `logo.jpg` مربّع).
- **معالجة الميدالية 3D:** قرص دائري بإطار ذهبي معدني (conic-gradient يحاكي اللمعان)، قرص داخلي رمادي متدرّج (لإبراز تفاصيل اللوقو الداكنة)، ظلال داخلية + إضاءة علوية.
- **حركة الافتتاح:** القطع تتجمّع من الجهات (فوق/تحت/يمين/يسار/وسط) وتتركّب على الميدالية، ثم طفو خفيف + نبضة حلقة ذهبية.

---

## 7. المكوّنات (UI Components)

- **الأزرار:** أساسي `--brand` (حبري، نص لؤلؤي)، ارتفاع `--control-lg`, زاوية `--radius-md`. ثانوي: حد `--border-default` بخلفية بطاقة. الذهبي `--accent` لأزرار الإبراز (نص `--ink-950`).
- **البطاقات:** `--surface-card`, زاوية `--radius-md/lg`, حد `--border-subtle`, ظل `--shadow-sm/md`.
- **الشرائح (Chips):** ارتفاع 36px, `--radius-full`, حد خفيف — للتصنيفات والفلاتر والموقع.
- **الحقول:** ارتفاع `--control-lg`, زاوية `--radius-md`, حد `--border-default`, تركيز `--shadow-focus`.
- **التبويبات السفلية (Bottom nav):** الرئيسية · حجوزاتي · الملف الشخصي.
- **الشيت السفلي (Bottom sheet):** زاوية علوية `--radius-2xl`, مقبض 40×4px, خلفية `--overlay`, دخول `tq-sheet-in`.
- **النجوم/التقييم:** ذهبي `--accent`.
- **التوست (Toast):** يظهر أسفل الشاشة, دخول `tq-toast-in`.

---

## 8. الشاشات (Screens / Flow)

1. **Splash** — خلفية حبرية متدرّجة + ميدالية اللوقو المتحركة → «ابدأ الآن».
2. **Onboarding** — صفحة واحدة، خلفية فاتحة، اللوقو على ميدالية + وصف الخدمات → تسجيل الدخول.
3. **Login** — رقم الجوال.
4. **OTP** — التحقق من الرمز.
5. **Home** — تحية + مُبدّل المدينة + بحث + تصنيفات + صالونات مميّزة/قريبة.
6. **Salon detail** — غلاف + خدمات + حلاقون + تقييمات → حجز.
7. **Booking flow** — اختيار الخدمة/الحلاق/الوقت/الموقع (صالون أو منزلي) + الدفع.
8. **Booking confirmed** — تأكيد.
9. **My bookings** — قادمة / مكتملة.
10. **Rate experience** — نجوم + وسوم.
11. **Profile** — بيانات المستخدم + الدفع.
12. **Salon dashboard** (دور صاحب صالون) — إدارة قائمة الخدمات (تُنشر للعميل).

---

## 9. قاعدة البيانات (Persistence)

تخزين محلي `localStorage` تحت مفتاح واحد `talluq_db_v1`، يتزامن تلقائيًا:
- **bookings** — الحجوزات (id, salon, service, barber, date, time, loc, status, upcoming, rated).
- **admin** — قائمة خدمات الصالون (name, price, dur) — **مربوطة**: عند الحفظ تصبح خدمات «غرفة الحبر» التي يحجز منها العميل.
- **phone** — رقم جوال المستخدم.
- **city** — المدينة المختارة (الرياض، جدة، الدمام، مكة، المدينة، الخبر، أبها، الطائف).
- **bkPay** — طريقة الدفع المفضّلة (مدى / Apple Pay / محفظة تألق).

---

## 10. بيانات العيّنة

- **الصالونات:** غرفة الحبر (رجال)، الشفرة الذهبية (رجال، منزلي)، قصّات التاج (عائلي)، المقص الصغير (أطفال)، دار اللؤلؤة (نساء)، ميزون لومي (نساء، منزلي).
- **الخدمات النموذجية:** قصة كلاسيكية 60 · فيد 75 · تشذيب لحية 45 · فوطة ساخنة 55 · شعر+لحية 95 (بالريال).
- **طرق الدفع:** بطاقة مدى (افتراضية) · Apple Pay · محفظة تألق (رصيد 120 ر.س).

---

*هذا الدليل يوثّق نظام تألق كما هو مطبّق في `Talluq App.dc.html`. جميع التوكنات معرّفة في `talluq/tokens/*.css`.*
